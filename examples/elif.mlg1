fn start() {
    let x = 30
    if x < 10 {
        color(255, 0, 0)
    }
    else { if x < 20 {
        color(0, 255, 0)
    }
    else { if x < 30 {
        color(255, 255, 0)
    }
    else {
        color(0, 0, 255)
    }}}
    rect(0, 0, WIDTH, HEIGHT)
}

fn tick() {}